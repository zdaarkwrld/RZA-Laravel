document.addEventListener('DOMContentLoaded', function() {
    const contentElement = document.querySelector('body > div.content');
    const decreaseButton = document.getElementById('decreaseText');
    const increaseButton = document.getElementById('increaseText');
    let fontSize = parseFloat(window.getComputedStyle(contentElement).fontSize);

    ```
    // ... (rest of the code remains the same)

    ```

    });
